{
  "name": "devsym-site",
  "version": "1.0.0",
  "description": "DevSym - Site vitrine statique",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "http-server -c-1 -p 3000",
    "start": "http-server -c-1 -p $PORT",
    "api": "node api-server.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
